import styles from "./styles.module.scss";

export function SubscribeButton() {
  return (
    <button type="button" onClick={() => {}} className={styles.subscribeButton}>
      Subscribe now
    </button>
  );
}
